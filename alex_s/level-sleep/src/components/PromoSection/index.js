import React from "react";

const PromoSection = ({ message }) => (
    <section className="promo-section">
        <div className="container">{message}</div>
    </section>
);

export default PromoSection;